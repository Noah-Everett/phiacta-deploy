# Environment files (may contain secrets or local overrides)
.env
.env.*
!.env.example

# Cloudflare tunnel credentials
cloudflared/

# Signing keys
keys/
*.pem

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Docker overrides
docker-compose.override.yml

# Prod compose (local-only, not tracked)
docker-compose.prod.yml
